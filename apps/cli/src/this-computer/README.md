# this-computer

**What.** This computer as penguin uses it: whether a terminal is here, the clipboard, `$EDITOR`, and how much RAM and load are left.

**Why.** The screens and commands ask the computer they sit on. That is not a penguin screen and not a run.
