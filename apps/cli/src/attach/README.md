# attach

**What.** Open a run as a viewer: the full-screen view on a terminal, or plain lines when piped.

**Why.** Closing a terminal never touches a run. This folder attaches a viewer to one run. It does not draw the screens, and it is not the machine.
