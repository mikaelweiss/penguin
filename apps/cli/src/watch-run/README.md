# watch-run

**What.** Open a run as a viewer: the full-screen view on a terminal, or plain lines when piped.

**Why.** Closing a terminal never touches a run. This folder watches one run. It does not draw the screens, and it is not this computer.
